# Base URL of the backend API (no trailing slash).
# Local development (default if unset): http://localhost:8080/api
# Production: set this in the Vercel dashboard to your deployed backend, e.g.
#   https://your-backend.up.railway.app/api
VITE_API_URL=http://localhost:8080/api
